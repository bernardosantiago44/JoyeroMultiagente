using UnityEngine;

public class TestSpawnSystem : MonoBehaviour
{
    // Triggers the spawn system testing when the MonoBehaviour is created
    void Start()
    {
        QATestingSystem.TestSpawnSystem();
    }
}